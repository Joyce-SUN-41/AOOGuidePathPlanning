# ============================================================
#  AOO Backend — AI / RAG 扩展依赖 (可选)
#  安装: pip install -r requirements-ai.txt
#  注意: torch 约 2GB，安装时间较长
# ============================================================

# RAG 知识库
chromadb>=0.5.0,<1.0.0
pypdf>=5.0.0,<6.0.0

# 嵌入模型（依赖 torch ~2GB）
sentence-transformers>=3.0.0,<4.0.0
